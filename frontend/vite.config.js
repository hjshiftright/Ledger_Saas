import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],

  server: {
    // Dev-server proxy: forwards /api/* to the local FastAPI backend so you
    // don't need to set VITE_API_BASE_URL when running `npm run dev`.
    proxy: {
      '/api': {
        target: 'http://127.0.0.1:8000',
        changeOrigin: true,
      },
    },
  },
})
